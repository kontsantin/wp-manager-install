#!/bin/bash
# ================================
# WordPress Manager с Docker
# Интерактивное управление проектами
# ================================

set -e

WWW_PATH="$HOME/www"
PROJECTS_FILE="$HOME/.wp-projects.txt"

# Цвета для вывода
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
NC='\033[0m' # No Color

# Функция вывода заголовка
print_header() {
    echo -e "${BLUE}================================${NC}"
    echo -e "${BLUE}  WordPress Manager${NC}"
    echo -e "${BLUE}================================${NC}"
    echo ""
}

# Функция выбора проекта из списка
select_project() {
    local filter_type="$1"  # all, running, stopped
    local projects=()
    local domains=()
    local ports=()
    local paths=()
    
    if [[ ! -f "$PROJECTS_FILE" ]] || [[ ! -s "$PROJECTS_FILE" ]]; then
        echo -e "${YELLOW}ℹ Нет созданных проектов${NC}"
        return 1
    fi
    
    # Собираем проекты в зависимости от фильтра
    while IFS='|' read -r name domain port path; do
        if [[ -d "$path" ]]; then
            local add_project=0
            
            if [[ "$filter_type" == "all" ]]; then
                add_project=1
            elif [[ "$filter_type" == "running" ]]; then
                if docker ps --format '{{.Names}}' | grep -q "^${name}_wp$"; then
                    add_project=1
                fi
            elif [[ "$filter_type" == "stopped" ]]; then
                if docker ps -a --format '{{.Names}}' | grep -q "^${name}_wp$" && ! docker ps --format '{{.Names}}' | grep -q "^${name}_wp$"; then
                    add_project=1
                fi
            fi
            
            if [[ $add_project -eq 1 ]]; then
                projects+=("$name")
                domains+=("$domain")
                ports+=("$port")
                paths+=("$path")
            fi
        fi
    done < "$PROJECTS_FILE"
    
    if [[ ${#projects[@]} -eq 0 ]]; then
        if [[ "$filter_type" == "running" ]]; then
            echo -e "${YELLOW}ℹ Нет запущенных проектов${NC}"
        elif [[ "$filter_type" == "stopped" ]]; then
            echo -e "${YELLOW}ℹ Нет остановленных проектов${NC}"
        else
            echo -e "${YELLOW}ℹ Нет проектов${NC}"
        fi
        return 1
    fi
    
    echo -e "${BLUE}Выберите проект:${NC}"
    echo ""
    
    local i=1
    for name in "${projects[@]}"; do
        local idx=$((i-1))
        echo -e "  ${GREEN}${i})${NC} ${name} (${domains[$idx]})"
        ((i++))
    done
    echo ""
    echo -e "  ${YELLOW}0)${NC} Отмена"
    echo ""
    
    read -p "Ваш выбор: " choice
    
    if [[ "$choice" == "0" ]]; then
        echo -e "${YELLOW}ℹ Отменено${NC}"
        return 1
    fi
    
    if [[ "$choice" =~ ^[0-9]+$ ]] && [[ $choice -ge 1 ]] && [[ $choice -le ${#projects[@]} ]]; then
        local idx=$((choice-1))
        SELECTED_PROJECT="${projects[$idx]}"
        SELECTED_DOMAIN="${domains[$idx]}"
        SELECTED_PORT="${ports[$idx]}"
        SELECTED_PATH="${paths[$idx]}"
        return 0
    else
        echo -e "${RED}❌ Неверный выбор${NC}"
        return 1
    fi
}

# Функция получения свободного порта
get_free_port() {
    local port=80
    
    # Проверяем порты начиная с 80
    while true; do
        # Проверяем через Docker - не занят ли порт контейнером
        if docker ps --format '{{.Ports}}' | grep -q "0.0.0.0:${port}->"; then
            ((port++))
            continue
        fi
        
        # Проверяем через netstat - не занят ли порт системой
        if netstat -tuln 2>/dev/null | grep -q ":${port} "; then
            ((port++))
            continue
        fi
        
        # Порт свободен
        echo "$port"
        return
    done
}

# Функция создания нового проекта
create_project() {
    echo -e "${GREEN}▶ Создание нового WordPress проекта${NC}"
    echo ""
    
    read -p "Введите название темы/проекта (пример: my-site): " THEME_NAME
    if [[ -z "$THEME_NAME" ]]; then
        echo -e "${RED}❌ Название не может быть пустым${NC}"
        return 1
    fi
    
    # Очищаем название от недопустимых символов (только латиница, цифры, дефис, подчёркивание)
    THEME_NAME=$(LC_ALL=C echo "$THEME_NAME" | sed 's/[^a-zA-Z0-9_-]//g' | tr '[:upper:]' '[:lower:]')
    
    if [[ -z "$THEME_NAME" ]]; then
        echo -e "${RED}❌ Название должно содержать только латинские буквы, цифры, дефис или подчёркивание${NC}"
        return 1
    fi
    
    echo -e "${BLUE}ℹ Используется название: ${THEME_NAME}${NC}"
    
    read -p "Введите домен (пример: my-site.local): " DOMAIN
    if [[ -z "$DOMAIN" ]]; then
        DOMAIN="${THEME_NAME}.local"
        echo -e "${YELLOW}ℹ Используется домен: ${DOMAIN}${NC}"
    fi
    
    WP_PATH="${WWW_PATH}/${THEME_NAME}"
    
    if [[ -d "$WP_PATH" ]]; then
        echo -e "${RED}❌ Проект '${THEME_NAME}' уже существует в ${WP_PATH}${NC}"
        return 1
    fi
    
    # Останавливаем все запущенные проекты
    echo ""
    echo -e "${YELLOW}🛑 Останавливаю все запущенные проекты...${NC}"
    
    if [[ -f "$PROJECTS_FILE" ]]; then
        while IFS='|' read -r name domain port path; do
            if [[ -d "$path" ]] && docker ps --format '{{.Names}}' | grep -q "^${name}_wp$"; then
                echo -e "   Останавливаю ${name}..."
                cd "$path" && docker compose stop 2>/dev/null
            fi
        done < "$PROJECTS_FILE"
    fi
    
    echo ""
    echo -e "${BLUE}📦 Создаю структуру проекта...${NC}"
    mkdir -p "$WP_PATH/wp-content/themes"
    mkdir -p "$WP_PATH/wp-content/plugins"
    mkdir -p "$WP_PATH/wp-content/uploads"
    
    # Генерация случайного пароля
    DB_PASS=$(openssl rand -base64 16 | tr -d "=+/" | cut -c1-16)
    SAFE_DB_NAME=$(echo "$THEME_NAME" | tr '-' '_' | tr '[:upper:]' '[:lower:]')
    DB_NAME="wp_${SAFE_DB_NAME}"
    DB_USER="${SAFE_DB_NAME}_user"
    
    # Получаем свободный порт
    PORT=$(get_free_port)
    
    echo -e "${BLUE}🐳 Создаю docker-compose.yml...${NC}"
    
    # Создание docker-compose.yml
    cat > "$WP_PATH/docker-compose.yml" <<'YAML'
version: '3.8'

services:
  db:
    image: mariadb:10.6
    container_name: PROJECTNAME_db
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: DBPASS
      MYSQL_DATABASE: DBNAME
      MYSQL_USER: DBUSER
      MYSQL_PASSWORD: DBPASS
    volumes:
      - db_data:/var/lib/mysql
    networks:
      - wp_network

  wordpress:
    image: wordpress:php8.1-apache
    container_name: PROJECTNAME_wp
    restart: unless-stopped
    depends_on:
      - db
    ports:
      - "PORTNUM:80"
    environment:
      WORDPRESS_DB_HOST: db:3306
      WORDPRESS_DB_USER: DBUSER
      WORDPRESS_DB_PASSWORD: DBPASS
      WORDPRESS_DB_NAME: DBNAME
      WORDPRESS_TABLE_PREFIX: wp_
      WORDPRESS_DEBUG: 1
    volumes:
      - ./wp-content:/var/www/html/wp-content
    networks:
      - wp_network

volumes:
  db_data:

networks:
  wp_network:
    driver: bridge
YAML

    # Заменяем плейсхолдеры
    sed -i "s/PROJECTNAME/${THEME_NAME}/g" "$WP_PATH/docker-compose.yml"
    sed -i "s/DBPASS/${DB_PASS}/g" "$WP_PATH/docker-compose.yml"
    sed -i "s/DBNAME/${DB_NAME}/g" "$WP_PATH/docker-compose.yml"
    sed -i "s/DBUSER/${DB_USER}/g" "$WP_PATH/docker-compose.yml"
    sed -i "s/PORTNUM/${PORT}/g" "$WP_PATH/docker-compose.yml"

    # Сохраняем информацию о проекте
    cat > "$WP_PATH/.project-info" <<INFO
PROJECT_NAME=${THEME_NAME}
DOMAIN=${DOMAIN}
PORT=${PORT}
DB_NAME=${DB_NAME}
DB_USER=${DB_USER}
DB_PASS=${DB_PASS}
CREATED=$(date '+%Y-%m-%d %H:%M:%S')
INFO

    # Скачиваем WordPress
    echo -e "${BLUE}📥 Скачиваю WordPress...${NC}"
    wget -q https://wordpress.org/latest.tar.gz -O /tmp/wp.tar.gz
    if [[ $? -ne 0 ]]; then
        echo -e "${RED}❌ Не удалось скачать WordPress${NC}"
        return 1
    fi
    
    # Распаковываем WordPress
    echo -e "${BLUE}📦 Распаковываю WordPress...${NC}"
    tar -xzf /tmp/wp.tar.gz -C "$WP_PATH" --strip-components=1
    if [[ $? -ne 0 ]]; then
        echo -e "${RED}❌ Не удалось распаковать WordPress${NC}"
        return 1
    fi
    
    # Удаляем wp-content из распакованного, чтобы монтировался локальный
    rm -rf "$WP_PATH/wp-content"
    
    # Создаём базовую тему
    THEME_DIR="$WP_PATH/wp-content/themes/$THEME_NAME"
    mkdir -p "$THEME_DIR"
    
    # Создаём style.css для темы
    cat > "$THEME_DIR/style.css" <<STYLE
/*
Theme Name: $THEME_NAME
Description: Custom WordPress theme for $THEME_NAME
Version: 1.0
Author: Your Name
*/

/* Основные стили */
body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
}

header {
    background: #333;
    color: white;
    padding: 1rem;
}

main {
    padding: 1rem;
}

footer {
    background: #333;
    color: white;
    text-align: center;
    padding: 1rem;
}
STYLE

    # Создаём index.php для темы
    cat > "$THEME_DIR/index.php" <<PHP
<?php get_header(); ?>

<main>
    <?php if (have_posts()) : ?>
        <?php while (have_posts()) : the_post(); ?>
            <article>
                <h2><a href="<?php the_permalink(); ?>"><?php the_title(); ?></a></h2>
                <div><?php the_excerpt(); ?></div>
            </article>
        <?php endwhile; ?>
    <?php else : ?>
        <p>Нет записей для отображения.</p>
    <?php endif; ?>
</main>

<?php get_footer(); ?>
PHP

    # Создаём header.php
    cat > "$THEME_DIR/header.php" <<PHP
<!DOCTYPE html>
<html <?php language_attributes(); ?>>
<head>
    <meta charset="<?php bloginfo('charset'); ?>">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><?php wp_title('|', true, 'right'); ?><?php bloginfo('name'); ?></title>
    <?php wp_head(); ?>
</head>
<body <?php body_class(); ?>>
    <header>
        <h1><?php bloginfo('name'); ?></h1>
        <p><?php bloginfo('description'); ?></p>
    </header>
PHP

    # Создаём footer.php
    cat > "$THEME_DIR/footer.php" <<PHP
    <footer>
        <p>&copy; <?php echo date('Y'); ?> <?php bloginfo('name'); ?></p>
    </footer>
    <?php wp_footer(); ?>
</body>
</html>
PHP

    # Создаём functions.php
    cat > "$THEME_DIR/functions.php" <<PHP
<?php
function $THEME_NAME_theme_setup() {
    add_theme_support('post-thumbnails');
    add_theme_support('title-tag');
}
add_action('after_setup_theme', '$THEME_NAME_theme_setup');

function $THEME_NAME_enqueue_styles() {
    wp_enqueue_style('$THEME_NAME-style', get_stylesheet_uri());
}
add_action('wp_enqueue_scripts', '$THEME_NAME_enqueue_styles');
?>
PHP
    
    # Добавляем в список проектов (с правильной кодировкой UTF-8)
    printf "%s|%s|%s|%s\n" "${THEME_NAME}" "${DOMAIN}" "${PORT}" "${WP_PATH}" >> "$PROJECTS_FILE"
    
    echo -e "${BLUE}🚀 Запускаю Docker контейнеры...${NC}"
    
    # Запуск docker-compose
    cd "$WP_PATH"
    if command -v docker >/dev/null && docker compose version >/dev/null 2>&1; then
        docker compose up -d
    elif command -v docker-compose >/dev/null; then
        docker-compose up -d
    else
        echo -e "${RED}❌ Docker compose не найден. Установите Docker Desktop.${NC}"
        return 1
    fi
    
    # Устанавливаем права на папку проекта для редактирования
    echo -e "${BLUE}🔧 Устанавливаю права на файлы...${NC}"
    sudo chown -R "$USER:$USER" "$WP_PATH"
    chmod -R 755 "$WP_PATH"
    
    # Настройка домена в /etc/hosts (WSL)
    echo -e "${BLUE}🔧 Настраиваю локальный домен...${NC}"
    
    # Проверяем, есть ли уже запись в hosts
    if ! grep -q "127.0.0.1.*${DOMAIN}" /etc/hosts 2>/dev/null; then
        echo "127.0.0.1 ${DOMAIN}" | sudo tee -a /etc/hosts >/dev/null
        echo -e "${GREEN}✅ Домен ${DOMAIN} добавлен в /etc/hosts (WSL)${NC}"
    else
        echo -e "${YELLOW}ℹ Домен ${DOMAIN} уже есть в /etc/hosts (WSL)${NC}"
    fi
    
    # Добавляем домен в Windows hosts файл автоматически
    WIN_HOSTS="/mnt/c/Windows/System32/drivers/etc/hosts"
    
    if [[ -f "$WIN_HOSTS" ]]; then
        if ! grep -q "127.0.0.1.*${DOMAIN}" "$WIN_HOSTS" 2>/dev/null; then
            echo ""
            echo -e "${YELLOW}═══════════════════════════════════════${NC}"
            echo -e "${BLUE}🔧 Добавляю домен ${DOMAIN} в Windows hosts...${NC}"
            echo -e "${YELLOW}Сейчас появится окно UAC - нажмите 'Да'${NC}"
            echo -e "${YELLOW}═══════════════════════════════════════${NC}"
            sleep 2
            
            # Пытаемся добавить через PowerShell с правами администратора
            if powershell.exe -Command "Start-Process powershell -Verb RunAs -ArgumentList '-NoProfile -Command \"Add-Content C:\\Windows\\System32\\drivers\\etc\\hosts ''127.0.0.1 ${DOMAIN}''\"' -Wait" 2>/dev/null; then
                echo -e "${GREEN}✅ Домен ${DOMAIN} добавлен в Windows hosts${NC}"
            else
                echo -e "${YELLOW}⚠ Не удалось добавить автоматически${NC}"
                echo -e "${BLUE}Выполните вручную в PowerShell от администратора:${NC}"
                echo -e "   ${GREEN}Add-Content C:\\Windows\\System32\\drivers\\etc\\hosts '127.0.0.1 ${DOMAIN}'${NC}"
            fi
        else
            echo -e "${GREEN}✅ Домен ${DOMAIN} уже есть в Windows hosts${NC}"
        fi
    fi
    
    echo ""
    echo -e "${GREEN}✅ Проект успешно создан!${NC}"
    echo ""
    echo -e "${YELLOW}═══════════════════════════════════════${NC}"
    echo -e "${GREEN}📋 Информация о проекте:${NC}"
    echo -e "${YELLOW}═══════════════════════════════════════${NC}"
    echo -e "  Проект:     ${THEME_NAME}"
    echo -e "  Домен:      ${DOMAIN}"
    echo -e "  Путь:       ${WP_PATH}"
    echo -e ""
    echo -e "${GREEN}🌍 Доступ к сайту:${NC}"
    if [[ "$PORT" == "80" ]]; then
        echo -e "  По домену:     ${GREEN}http://${DOMAIN}${NC}"
        echo -e "  По IP:         http://localhost"
    else
        echo -e "  По домену:     ${GREEN}http://${DOMAIN}:${PORT}${NC}"
        echo -e "  По IP:         http://localhost:${PORT}"
    fi
    echo -e ""
    echo -e "${GREEN}🔐 База данных:${NC}"
    echo -e "  БД:         ${DB_NAME}"
    echo -e "  Юзер:       ${DB_USER}"
    echo -e "  Пароль:     ${DB_PASS}"
    echo -e ""
    if [[ "$PORT" == "80" ]]; then
        echo -e "${BLUE}💡 Откройте ${GREEN}http://${DOMAIN}${BLUE} в браузере Windows${NC}"
    else
        echo -e "${BLUE}💡 Откройте ${GREEN}http://${DOMAIN}:${PORT}${BLUE} в браузере Windows${NC}"
    fi
    echo -e "${BLUE}   и пройдите установку WordPress${NC}"
    echo -e "${YELLOW}═══════════════════════════════════════${NC}"
    echo ""
}

# Функция списка проектов
list_projects() {
    echo -e "${GREEN}▶ Список проектов${NC}"
    echo ""
    
    if [[ ! -f "$PROJECTS_FILE" ]] || [[ ! -s "$PROJECTS_FILE" ]]; then
        echo -e "${YELLOW}ℹ Нет созданных проектов${NC}"
        return
    fi
    
    echo -e "${YELLOW}═══════════════════════════════════════════════════════════${NC}"
    printf "%-20s %-25s %-10s %s\n" "ПРОЕКТ" "ДОМЕН" "ПОРТ" "СТАТУС"
    echo -e "${YELLOW}═══════════════════════════════════════════════════════════${NC}"
    
    while IFS='|' read -r name domain port path; do
        if [[ -d "$path" ]]; then
            # Проверяем статус контейнера
            if docker ps --format '{{.Names}}' | grep -q "^${name}_wp$"; then
                status="${GREEN}●${NC} Running"
            elif docker ps -a --format '{{.Names}}' | grep -q "^${name}_wp$"; then
                status="${YELLOW}●${NC} Stopped"
            else
                status="${RED}●${NC} Not found"
            fi
            printf "%-20s %-25s %-10s %b\n" "$name" "$domain" "$port" "$status"
        fi
    done < "$PROJECTS_FILE"
    
    echo -e "${YELLOW}═══════════════════════════════════════════════════════════${NC}"
    echo ""
}

# Функция остановки проекта
stop_project() {
    echo -e "${YELLOW}▶ Остановка проекта${NC}"
    echo ""
    
    if ! select_project "running"; then
        return
    fi
    
    cd "$SELECTED_PATH"
    
    if command -v docker >/dev/null && docker compose version >/dev/null 2>&1; then
        docker compose stop
    elif command -v docker-compose >/dev/null; then
        docker-compose stop
    fi
    
    echo ""
    echo -e "${GREEN}✅ Проект '${SELECTED_PROJECT}' остановлен${NC}"
}

# Функция запуска проекта
start_project() {
    echo -e "${GREEN}▶ Запуск проекта${NC}"
    echo ""
    
    if ! select_project "stopped"; then
        return
    fi
    
    cd "$SELECTED_PATH"
    
    if command -v docker >/dev/null && docker compose version >/dev/null 2>&1; then
        docker compose start
    elif command -v docker-compose >/dev/null; then
        docker-compose start
    fi
    
    echo ""
    echo -e "${GREEN}✅ Проект '${SELECTED_PROJECT}' запущен${NC}"
    if [[ "$SELECTED_PORT" == "80" ]]; then
        echo -e "${BLUE}🌍 Доступ: http://${SELECTED_DOMAIN}${NC}"
    else
        echo -e "${BLUE}🌍 Доступ: http://${SELECTED_DOMAIN}:${SELECTED_PORT}${NC}"
    fi
}

# Функция открытия проекта в VS Code
open_in_vscode() {
    echo -e "${BLUE}▶ Открыть проект в VS Code${NC}"
    echo ""
    
    if ! select_project "all"; then
        return
    fi
    
    echo ""
    echo -e "${BLUE}Выберите, что открыть:${NC}"
    echo ""
    echo "  1) Весь сайт (все исходники WordPress)"
    echo "  2) Только wp-content (темы, плагины, загрузки)"
    echo "  3) Только тема проекта"
    echo ""
    read -p "Ваш выбор (1, 2 или 3): " open_choice
    
    case $open_choice in
        1)
            open_path="$SELECTED_PATH"
            ;;
        2)
            if [[ -d "$SELECTED_PATH/wp-content" ]]; then
                open_path="$SELECTED_PATH/wp-content"
            else
                echo -e "${YELLOW}⚠ wp-content не найден, открываю весь сайт${NC}"
                open_path="$SELECTED_PATH"
            fi
            ;;
        3)
            theme_path="$SELECTED_PATH/wp-content/themes/$SELECTED_PROJECT"
            if [[ -d "$theme_path" ]]; then
                open_path="$theme_path"
            else
                echo -e "${YELLOW}⚠ Тема '$SELECTED_PROJECT' не найдена, открываю wp-content${NC}"
                open_path="$SELECTED_PATH/wp-content"
            fi
            ;;
        *)
            echo -e "${YELLOW}ℹ Выбран весь сайт${NC}"
            open_path="$SELECTED_PATH"
            ;;
    esac
    
    echo ""
    echo -e "${BLUE}🚀 Открываю ${open_path} в VS Code...${NC}"
    
    # Пытаемся открыть через code в WSL (предпочтительно)
    if command -v code >/dev/null 2>&1; then
        cd "$open_path" && code . >/dev/null 2>&1
        if [[ $? -eq 0 ]]; then
            echo -e "${GREEN}✅ VS Code запущен${NC}"
            echo -e "${BLUE}   Папка: ${open_path}${NC}"
            return
        else
            echo -e "${YELLOW}⚠ Не удалось открыть через WSL code, пробую через Windows...${NC}"
        fi
    fi
    
    # Fallback: через PowerShell из Windows
    local wsl_distro=$(lsb_release -si 2>/dev/null || echo "Ubuntu")
    local win_path=$(wslpath -w "$open_path" 2>/dev/null || echo "\\\\wsl.localhost\\${wsl_distro}${open_path}")
    
    if command -v powershell.exe >/dev/null 2>&1; then
        powershell.exe -Command "code '${win_path}'" >/dev/null 2>&1
        if [[ $? -eq 0 ]]; then
            echo -e "${GREEN}✅ VS Code запущен${NC}"
            echo -e "${BLUE}   Папка: ${win_path}${NC}"
        else
            echo -e "${RED}❌ Не удалось открыть VS Code через PowerShell${NC}"
            echo -e "${YELLOW}Попробуйте открыть вручную: ${win_path}${NC}"
        fi
    else
        echo -e "${RED}❌ PowerShell не найден${NC}"
        echo -e "${YELLOW}Откройте вручную: ${win_path}${NC}"
    fi
}

# Функция открытия сайта в браузере
open_in_browser() {
    echo -e "${BLUE}▶ Открыть сайт в браузере${NC}"
    echo ""
    
    if ! select_project "running"; then
        return
    fi
    
    if [[ "$SELECTED_PORT" == "80" ]]; then
        URL="http://${SELECTED_DOMAIN}"
    else
        URL="http://${SELECTED_DOMAIN}:${SELECTED_PORT}"
    fi
    
    echo ""
    echo -e "${BLUE}🌍 Открываю ${URL}...${NC}"
    
    # Пытаемся открыть в Windows браузере
    if command -v powershell.exe >/dev/null 2>&1; then
        powershell.exe -Command "Start-Process '${URL}'" 2>/dev/null &
        echo -e "${GREEN}✅ Браузер запущен${NC}"
    elif command -v cmd.exe >/dev/null 2>&1; then
        cmd.exe /c start "${URL}" 2>/dev/null &
        echo -e "${GREEN}✅ Браузер запущен${NC}"
    else
        echo -e "${YELLOW}⚠ Не удалось открыть браузер автоматически${NC}"
        echo -e "${BLUE}Откройте вручную: ${GREEN}${URL}${NC}"
    fi
}

# Функция удаления проекта
remove_project() {
    echo -e "${RED}▶ Удаление проекта${NC}"
    echo ""
    
    if ! select_project "all"; then
        return
    fi
    
    echo ""
    echo -e "${YELLOW}⚠ Внимание! Это удалит все файлы и базу данных проекта '${SELECTED_PROJECT}'.${NC}"
    read -p "Вы уверены? Введите 'yes' для подтверждения: " CONFIRM
    
    if [[ "$CONFIRM" != "yes" ]]; then
        echo -e "${BLUE}ℹ Отменено${NC}"
        return 0
    fi
    
    echo ""
    echo -e "${BLUE}🗑 Удаляю проект...${NC}"
    
    cd "$SELECTED_PATH"
    
    # Останавливаем и удаляем контейнеры
    if command -v docker >/dev/null && docker compose version >/dev/null 2>&1; then
        docker compose down -v 2>/dev/null || true
    elif command -v docker-compose >/dev/null; then
        docker-compose down -v 2>/dev/null || true
    fi
    
    # Удаляем папку проекта (с sudo, т.к. Docker создаёт файлы от root)
    cd "$HOME"
    sudo rm -rf "$SELECTED_PATH"
    
    # Удаляем из списка проектов
    if [[ -f "$PROJECTS_FILE" ]]; then
        sed -i "/^${SELECTED_PROJECT}|/d" "$PROJECTS_FILE"
    fi
    
    # Удаляем из /etc/hosts (WSL)
    if grep -q "127.0.0.1.*${SELECTED_DOMAIN}" /etc/hosts 2>/dev/null; then
        sudo sed -i "/127.0.0.1.*${SELECTED_DOMAIN}/d" /etc/hosts 2>/dev/null || true
    fi
    
    # Удаляем домен из Windows hosts файла автоматически
    WIN_HOSTS="/mnt/c/Windows/System32/drivers/etc/hosts"
    
    if [[ -f "$WIN_HOSTS" ]]; then
        if grep -q "127.0.0.1.*${SELECTED_DOMAIN}" "$WIN_HOSTS" 2>/dev/null; then
            echo ""
            echo -e "${YELLOW}═══════════════════════════════════════${NC}"
            echo -e "${BLUE}🔧 Удаляю домен ${SELECTED_DOMAIN} из Windows hosts...${NC}"
            echo -e "${YELLOW}Сейчас появится окно UAC - нажмите 'Да'${NC}"
            echo -e "${YELLOW}═══════════════════════════════════════${NC}"
            sleep 2
            
            # Пытаемся удалить через PowerShell с правами администратора
            powershell.exe -Command "Start-Process powershell -Verb RunAs -ArgumentList '-NoProfile -Command \"(Get-Content C:\\Windows\\System32\\drivers\\etc\\hosts) -notmatch ''${SELECTED_DOMAIN}'' | Set-Content C:\\Windows\\System32\\drivers\\etc\\hosts\"' -Wait" 2>/dev/null
            if [[ $? -eq 0 ]]; then
                echo -e "${GREEN}✅ Домен ${SELECTED_DOMAIN} удалён из Windows hosts${NC}"
            else
                echo -e "${YELLOW}⚠ Не удалось удалить автоматически${NC}"
                echo -e "${BLUE}Удалите вручную из C:\\Windows\\System32\\drivers\\etc\\hosts строку:${NC}"
                echo -e "   ${GREEN}127.0.0.1 ${SELECTED_DOMAIN}${NC}"
            fi
        else
            echo -e "${GREEN}✅ Домен ${SELECTED_DOMAIN} уже удалён из Windows hosts${NC}"
        fi
    fi
    
    echo ""
    echo -e "${GREEN}✅ Проект '${SELECTED_PROJECT}' успешно удалён${NC}"
}

# Главное меню
main_menu() {
    while true; do
        print_header
        echo "Выберите действие:"
        echo ""
        echo "  1) Создать новый проект"
        echo "  2) Список проектов"
        echo "  3) Запустить проект"
        echo "  4) Остановить проект"
        echo "  5) Удалить проект"
        echo "  6) Открыть проект в VS Code"
        echo "  7) Открыть сайт в браузере"
        echo "  0) Выход"
        echo ""
        read -p "Ваш выбор: " choice
        echo ""
        
        case $choice in
            1) create_project ;;
            2) list_projects ;;
            3) start_project ;;
            4) stop_project ;;
            5) remove_project ;;
            6) open_in_vscode ;;
            7) open_in_browser ;;
            0) 
                echo -e "${BLUE}👋 До свидания!${NC}"
                exit 0
                ;;
            *) 
                echo -e "${RED}❌ Неверный выбор${NC}"
                ;;
        esac
        
        echo ""
        read -p "Нажмите Enter для продолжения..."
        clear
    done
}

# Создаём директорию для проектов если не существует
mkdir -p "$WWW_PATH"

# Запускаем главное меню
clear
main_menu
